mixin videoBlock(video = {})
    .videoBlock
        video.videoBlock__thumbnail(src=video.videoFile)
        h4.videoBlock__title=video.title
        h6.videoBlock__views=video.views


//- mixin은 pug의 함수이고 하나의 파라미터(object)가 필요함